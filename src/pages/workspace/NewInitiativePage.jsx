import { useEffect, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { supabase } from '../../lib/supabase'
export default function NewInitiativePage() { const [goals,setGoals]=useState([]); const [form,setForm]=useState({goal_id:'',title:'',description:''}); const [error,setError]=useState(''); const navigate=useNavigate(); useEffect(()=>{supabase.from('workspace_members').select('workspace_id').eq('status','active').order('created_at').limit(1).single().then(async({data,error:e})=>{if(e)return setError(e.message); const r=await supabase.from('goals').select('id,title').eq('workspace_id',data.workspace_id).order('title'); if(r.error)setError(r.error.message); else {setGoals(r.data||[]);setForm(f=>({...f,goal_id:r.data?.[0]?.id||''}))}})},[]); async function submit(e){e.preventDefault();const m=await supabase.from('workspace_members').select('workspace_id').eq('status','active').order('created_at').limit(1).single();const u=await supabase.auth.getUser();const r=await supabase.from('initiatives').insert({...form,workspace_id:m.data.workspace_id,created_by:u.data.user.id}).select('id').single();if(r.error)setError(r.error.message);else navigate('/admin/planning')} return <main className="min-h-screen bg-slate-950 px-5 py-8 text-white md:px-10 md:py-12"><div className="mx-auto max-w-3xl"><Link className="text-orange-300" to="/admin/planning">← Planning</Link><h1 className="mt-10 text-4xl font-semibold">New initiative</h1><form onSubmit={submit} className="mt-8 space-y-5 rounded-3xl border border-white/10 bg-white/[0.04] p-6"><select required value={form.goal_id} onChange={e=>setForm({...form,goal_id:e.target.value})} className="w-full rounded-xl bg-slate-900 p-3">{goals.map(g=><option key={g.id} value={g.id}>{g.title}</option>)}</select><input required placeholder="Initiative title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})} className="w-full rounded-xl bg-slate-900 p-3"/><textarea placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} className="w-full rounded-xl bg-slate-900 p-3" rows="5"/>{error&&<p className="text-rose-300">{error}</p>}<button className="rounded-full bg-orange-300 px-5 py-3 font-semibold text-slate-950">Create initiative</button></form></div></main> }
