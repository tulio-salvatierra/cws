# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets — single .env for local dev; sync with Vercel via npm run env:pull
.env
.env.local
.env.production
.env.*.local

# Separate local projects — not part of this repo
ayuda-terremoto-venezuela/

# Agent / planning artifacts
# Shared project instructions may live in `agents/` or `.agents/` and should be tracked.
# Keep only local, machine-specific overrides out of git.
.agents.local/
agents.local/
.codex/
.claude/
.superpowers/
.tmp/
skills-lock.json
