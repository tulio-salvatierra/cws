# Task Ledger

| Task ID | Agent | Status | Result | Next |
| --- | --- | --- | --- | --- |
| CWS-AUDIT-001 | Codex | Completed | Repo audited against product definition | Claude architecture review |
| CWS-DB-DESIGN-001 | Codex | Completed | Schema foundation designed; channel and role rules approved | Implement workspace foundation |
| CWS-DB-FOUNDATION-001 | Codex | Completed with limitation | Workspace and membership migration added; build and targeted tests pass | Run migration and RLS tests in Supabase |
| CWS-DB-VALIDATION-001 | Codex | Completed | Migrations 001–007 and two-user RLS scenarios pass on managed Supabase | Implement channels |
| CWS-DB-CHANNELS-001 | Codex | Completed | Workspace channels and RLS validated through migration 008 | Implement campaigns and content variants |
| CWS-DB-CONTENT-001 | Codex | Completed | Campaigns and independent variants validated through migration 009 | Implement variant approvals |
| CWS-DB-APPROVALS-001 | Codex | Completed | Variant approval lifecycle and owner-only review validated through migration 010 | Implement agent runs or seed CWS-001 |
| CWS-DB-AGENT-RUNS-001 | Codex | Completed | Auditable Ask/Propose runs validated through migration 011; Execute remains blocked | Seed CWS-001 or implement read-only Ask API |
| CWS-DB-CONSOLIDATE-001 | Codex | Completed with follow-up | Five database branches are merged on `main`; migrations 007–012 are present and migration 012 is applied to staging | CWS-ADMIN-CONSOLIDATE-003 hardens remaining function security and consolidates routes |
| CWS-DB-MANAGED-VALIDATION-001 | Cursor | Completed with limitation | Staging `001`–`006` confirmed applied; two-user workspace RLS checklist passed; literal `db push` blocked by remote-only migration `20260730231228` | Align local history with remote migration `20260730231228`, then finish remaining consolidate/advisor work |
| CWS-DB-MIGRATION-RECONCILE-001 | Cursor | Completed with follow-up required | Remote-only `20260730231228` is meaningful goals/OS schema (KEEP, not repair); `db pull` blocked by history conflict and dump needs Docker | Add local `20260730231228_goals_...sql` from dump, then confirm clean `db push` |
| CWS-WEB-MARKETING-MOTION-001 | Codex | Completed | Marketing video/motion update validated; Preview Supabase config and admin login verified | Reconcile remote migration, then repair stale admin tests |
| CWS-WEB-WORKSPACE-001 | Codex | Completed | Protected `/workspace` dashboard deployed in Preview; staging seed and Linux build fix validated | Add detail views/navigation, then reconcile remote migration |
| CWS-WEB-UNIFIED-DASHBOARD-001 | Codex | Completed | Admin overview became the unified dashboard entry point with live workspace counts | Consolidate the remaining CWS OS routes under `/admin` |
| CWS-WEB-KNOWLEDGE-OVERVIEW-001 | Codex | Completed | Added the workspace knowledge overview for decisions and learnings | Continue the unified admin route work |
| CWS-WEB-PLANNING-OVERVIEW-001 | Codex | Completed | Added the planning overview for goals, initiatives, and projects | Add creation workflows |
| CWS-WEB-AGENT-RUNS-OVERVIEW-001 | Codex | Completed | Added the agent-runs overview to the workspace dashboard | Include the page in the consolidated admin navigation |
| CWS-WEB-COUNTS-001 | Codex | Completed | Added live workspace counts to the admin overview | Keep counts aligned with RLS-visible records |
| CWS-WEB-GOAL-CREATION-001 | Codex | Completed | Added goal creation workflow | Consolidate the planning routes under `/admin` |
| CWS-WEB-INITIATIVE-CREATION-001 | Codex | Completed | Added initiative creation workflow | Consolidate the planning routes under `/admin` |
| CWS-WEB-PROJECT-CREATION-001 | Codex | Completed | Added project creation workflow | Add project detail navigation |
| CWS-WEB-PROJECT-DETAIL-001 | Codex | Completed | Added project detail and linked-task view | Consolidate all workspace routes under `/admin` |
| CWS-ADMIN-CONSOLIDATE-003 | Codex | Completed locally; deployment pending | Synced lockfile, repaired stale tests, consolidated routes under `/admin`, applied the private-schema helper migration to staging, and added the protected `/admin/channels` overview with redirect/navigation smoke coverage; 47 policies use private helpers and member/non-member RLS checks pass | Push `dbd9396`, deploy, and manually smoke-test `/admin/channels` plus the legacy `/workspace` redirect |
| CWS-PILOT-READINESS-004 | Codex | In progress — approval guard pushed | Added campaign and variant lifecycle controls, immutable-history revision re-submission, workspace-owned decision/learning creation, and an explicit confirmation guard for single-owner review decisions through `0ecd8e3`; 67 tests pass | Confirm deployment and validate a fresh Spanish approval/revision cycle; then design the future export/outcome migration from the live findings |
| CWS-VERCEL-RELIABILITY-005 | Codex | Completed and deployed | Guarded stale-chunk recovery and correct asset/API 404 behavior are live; the intake webhook has bounded/logged execution; 62 tests, both builds, and safe production HTTP checks pass | Tulio submits one intentional intake, then resume the approval revision-resolution loop |
| CWS-VERCEL-RELIABILITY-006 | Codex | Completed and live-verified | Managed background relay is deployed; two client updates reached Google Sheets with HTTP 200, and production serves the corrected campaign pattern | Add a friendly duplicate campaign-code message, then resume the approval revision cycle |
| CWS-CAMPAIGN-UX-007 | Codex | Pushed; deployment verification pending | Maps Supabase `23505` to an accessible workspace-scoped duplicate campaign-code message in `22c3373` while retaining database enforcement; 68 tests pass | Confirm deployment, verify one repeated code, then resume approval history and the revision cycle |
| CWS-N8N-ASSESS-006 | Codex | Pushed — read-only assessment | Audited eight live workflows, credentials, retained executions, Production configuration, and live schema; five workflows are published but dormant, the publisher is inactive/incomplete, and no social publishing is scheduled | Tulio decides revive or retire; if reviving, choose the authoritative database and ratify the WF4/WF5 contracts before any repair |
| CWS-RETURN-PATH-007 | Codex | Completed, deployed, and UI-verified | Added authenticated idempotent publish-event recording, workspace-member outcome access, and the `/admin/published` content tab; staging security/manual checks, all 76 tests, Production deployment, and the signed-in empty-state smoke test pass | Wire the selected publisher to the return endpoint before outbound publishing, then validate one real publish, retry, and member outcome update |
| CWS-CHANNEL-BRIEF-008 | Codex | Completed, deployed, and live-verified | Added independent versioned EN/ES channel briefs, one-active-per-language enforcement, workspace RLS, and immutable optional `published_posts.brief_version`; four real briefs are active, all 82 tests pass, and Production returned 201 then idempotent 200 with version 1 | Review the brief wording, then build a non-publishing generation path that reads the active brief and records its version |
| CWS-GENERATION-TEST-009 | Codex | Merged and deployed; signed-in verification pending | Added an authenticated CWS-English non-publishing generator that records the exact active brief snapshot/version in a `propose` run and saves the result as `needs_review`; all 87 tests, lint, build, and a live OpenAI smoke request pass; PR #13 is merged and Production is Ready | Verify one signed-in generation, then design proposal promotion |
| CWS-GENERATION-REVIEW-010 | Codex | Merged, deployed, and unauthenticated guard verified | Added owner-only accept/reject review; acceptance atomically creates one provenance-linked draft variant, rejection creates none, all 94 tests plus managed staging security/idempotency checks pass, and Production is Ready | Review the real generated run, verify its linked draft if accepted, then design the separate approval-request step |
| CWS-GENERATION-APPROVAL-011 | Codex | Merged and deployed; real request pending | Added immutable content snapshots and atomic variant-status synchronization to the existing request/review flow; all 95 tests and managed staging lifecycle, evidence, and tenant checks pass, and Production is Ready | Submit the real accepted variant for review, then verify its pending snapshot before the owner decision |
| CWS-EXPORT-HANDOFF-012 | Codex | Completed, deployed, and live-verified | Added an explicit non-publishing approved-to-exported handoff with authenticated attribution, immutable final snapshot, historical evidence markers, confirmation, and actionable missing-field feedback; all 97 tests and managed staging checks pass, and the first real export evidence is locked | Decide whether caption `test` is acceptable evidence or build a versioned correction/re-export path |
| CWS-EXPORT-REVISION-013 | Codex | Merged and deployed; real correction pending | Added append-only workspace-owned export versions, immutable correction evidence, explicit minimal grants, automatic future version-1 capture, export history, and a confirmed correction UI; all 98 tests and managed staging security/lifecycle checks pass, PR #18 is merged, and Production is Ready while the real version 1 remains unchanged | Record and verify the real corrected version 2 without publishing or n8n |
| CWS-PILOT-STATUS-RECONCILE-014 | Codex | Completed and merged | PR #21 merged as `3abf34a`; exactly three non-exported pre-trigger approval/status mismatches were reconciled while export evidence stayed untouched | Use the approved-content revision path before replacing English pilot test content |
| CWS-APPROVED-REVISION-015 | Codex | Completed, merged, deployed, and staging migrations applied | PR #22 merged as `2d6ce5f`; owner-confirmed append-only revisions, database-enforced locks and fresh re-review, minimal RLS/grants, and UI confirmation are live; all 99 tests and managed lifecycle probes pass without changing pilot rows | Revise and freshly approve the real English CWS-001 content |
| CWS-FIRST-RECORD-011 | Codex | Completed locally in two commits; not pushed | All eight Production return-path checks passed, the temporary row was deleted and the table is empty, and one documented command now records an actual manual post; 103 tests and required checks pass | After a real publication, run the recorder and verify the first durable row in `/admin/published` |
| CWS-TEST-DATA-ARCHIVE-016 | Codex | Completed, merged, deployed, and Production records classified | PR #25 merged as `bfdd74e`; five approved test campaigns and six approved test variants are reversibly archived, normal views retain only `CWS-001` and its canonical masters, and seven approvals plus seven export versions remain intact | Replace `CWS-001-EN-MASTER` through revision, fresh review, approval, and confirmed export without publishing or n8n |
| CWS-N8N-DRY-RUN-017 | Codex | Implemented locally; n8n live; app deployment pending | WF1/WF5 are unpublished; a dedicated authenticated dry-run workflow returned a matching 200 acknowledgement; owner-only app dispatch records `agent_runs` and cannot publish; 112 tests pass | Commit/push, redeploy Production, run the archived final-test variant, and verify one completed agent run with no publication row |
| CWS-TOPIC-LEDGER-019 | Codex | Completed; staging migration and approved queue verified | Added workspace-scoped channel topic vocabulary, append-only coverage evidence, mutable proposal queue, security-invoker performance/Markdown projections, and bilingual CWS seed rows from EN/ES v1 briefs; pgTAP coverage, all 118 Vitest tests, lint, and build pass; staging has 6 seeded topics per locale and four fully attributed approved non-publishing proposals | Create a real content variant for one approved proposal, then record coverage only after that content exists |
